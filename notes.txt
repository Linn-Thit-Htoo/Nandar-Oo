- HTML
- CSS
- Javascript
- Boostratp

projects

- mini calcaluator project
- fully functional calcaluator project
- local stroage CRUD
- project using Boostratp

2 kinds => static & dynamic websites

HTML => HyperText Markup Language

- opening tag(<p>) & closing tag (</p>)
- self closing tag

- inline level elements
- block level elements

html => head, body
head => meta, title
body => h, p, span


******* Day 2 *******

- 2 kinds => key-value pairs attribute (name = "Linn Thit")
          => standalone attribute (required, readonly, disabled, selected)

comment => ctrl + /
        => cmd + /

bdo -> default => ltr (left to right)
               => rtl (right to left)

ctrl + space (code suggestion)

a => anchor
target key-value pair attribute

cdn => content delivery network

******* Day 3 *******

img => image
attributes => src (sourcce), width, height (unit => px, em, vh, %), alt (alternative text)

input types => text, tel, number, email, password, date, checkbox, radio, submit, range, file, url

thead => table header
tbody => table body
tr => table row
th => table heading
td => table data